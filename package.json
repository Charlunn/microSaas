{
  "name": "micro-saas-factory",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "generate:child": "tsx scripts/generate-child-app.ts"
  },
  "devDependencies": {
    "turbo": "^2.0.14",
    "tsx": "^4.20.6",
    "typescript": "^5.6.3"
  }
}
